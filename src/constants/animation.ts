/** Duration for each tile-to-tile orb transition. */
export const ORB_STEP_MS = 250;

/** Maximum number of trail tiles shown behind the orb. */
export const ORB_TRAIL_MAX_LENGTH = 10;

/** Fade duration for orb trail tiles. */
export const ORB_TRAIL_FADE_MS = 500;

/** Pixel gap between board tiles — must match BoardGrid. */
export const BOARD_TILE_GAP_PX = 8;
