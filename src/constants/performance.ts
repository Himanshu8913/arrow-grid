/** Board sizes at or above this use content-visibility tile optimization. */
export const VIRTUAL_BOARD_SIZE_THRESHOLD = 9;

/** Maximum simultaneous one-shot SFX voices. */
export const MAX_CONCURRENT_SFX_VOICES = 8;

/** Debounce window for hover sounds (mirrors audio constant). */
export const PERFORMANCE_HOVER_MS = 90;
