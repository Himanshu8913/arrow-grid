/** Minimum XP awarded from a completed match. */
export const MIN_MATCH_XP = 20;

/** Maximum XP awarded from a completed match. */
export const MAX_MATCH_XP = 500;

/** XP divisor applied to match score. */
export const XP_SCORE_DIVISOR = 5;

/** Coins awarded for a win. */
export const COINS_WIN = 30;

/** Coins awarded for a loss. */
export const COINS_LOSS = 10;
